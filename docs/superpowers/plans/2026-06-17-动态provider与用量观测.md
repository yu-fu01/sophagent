# 动态 Provider 与用量观测 实现计划（REQ3.1–3.4）

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 让 admin 在 Web 上动态管理 provider（含加密入库的 apikey）、按 provider 拉模型列表、在会话里切换 model 与思考/快速模式，并在聊天界面显示 token 用量、上下文长度与缓存命中率。

**架构：** 引入模块级 `ProviderRegistry` 单例（yaml 内置只读 + DB 托管合并，DB 优先），统一 provider 解析与客户端缓存；apikey 用 `config.secret` 派生的 Fernet 密钥加密入库、响应掩码；会话新增覆盖列承载临时切换；适配器读取缓存 token 并新增 `turn_usage` 事件，前端加状态栏与每轮标签。

**技术栈：** Python 3.12 / FastAPI / aiosqlite / pydantic / `cryptography`(Fernet) / openai SDK / anthropic SDK / 单文件原生 JS 前端。

参考规格：`docs/superpowers/specs/2026-06-17-动态provider与用量观测-design.md`

---

## 文件结构

| 文件 | 职责 |
|---|---|
| `sophagent/crypto.py`（新） | Fernet 加解密 + key 掩码 |
| `sophagent/usage.py`（新） | `cache_hit_percent` 纯函数 |
| `sophagent/providers/registry.py`（新） | `ResolvedProvider` + `ProviderRegistry` 单例（合并/解析/客户端缓存/模型列表缓存） |
| `sophagent/providers/base.py` | `Provider.chat()` 增 `thinking` 参数 |
| `sophagent/providers/openai_provider.py` | 缓存 token 读取；`reasoning_effort` 映射 + 容错 |
| `sophagent/providers/anthropic_provider.py` | 缓存 token 读取；`cache_control`；`thinking` 映射 |
| `sophagent/providers/__init__.py` | `get_provider` 委托 registry |
| `sophagent/models.py` | `AssistantTurn` 加 cache 字段；pydantic：`ProviderCreate/ProviderPatch/SessionOverridePatch` |
| `sophagent/db.py` | `providers` 表 + sessions 覆盖列迁移 + provider CRUD + session override 方法 |
| `sophagent/config.py` | `ProviderConfig` 加 `source` 标记（builtin） |
| `sophagent/main.py` | 启动 `init_registry(db, cfg)` |
| `sophagent/agent/runtime.py` | `build_runner` 注入 effective provider/model/thinking |
| `sophagent/agent/loop.py` | 用 registry 解析；发 `turn_usage`；`done` 扩展 |
| `sophagent/api/provider_routes.py`（新） | provider CRUD + 模型列表 |
| `sophagent/api/__init__.py` | 挂载 provider 路由 |
| `sophagent/api/agent_routes.py` | `_validate` 改查 registry |
| `sophagent/api/session_routes.py` | `PATCH` 覆盖列；chat 读覆盖传入 runner |
| `web/index.html` | Provider 卡片、model 下拉、会话工具栏、用量状态栏 + 每轮标签 |
| `tests/conftest.py` | `EchoProvider.chat` 加 `**kwargs`；脚本化 cache 字段 |
| `tests/test_crypto.py`（新） | 加解密/掩码 |
| `tests/test_providers_api.py`（新） | CRUD/掩码/权限/模型列表 |
| `tests/test_registry.py`（新） | 合并/优先级/客户端缓存失效 |
| `tests/test_overrides_usage.py`（新） | 会话覆盖/思考参数/用量事件 |

---

# 里程碑 M1：Provider 持久化（crypto + 表 + registry + CRUD + 接入）

## 任务 1：crypto 加解密模块

**文件：**
- 创建：`sophagent/crypto.py`
- 测试：`tests/test_crypto.py`

- [ ] **步骤 1：编写失败的测试**

```python
# tests/test_crypto.py
from sophagent.crypto import encrypt, decrypt, mask_key

SECRET = "0" * 64

def test_encrypt_decrypt_roundtrip():
    token = encrypt("sk-secret-1234", SECRET)
    assert token != "sk-secret-1234"
    assert decrypt(token, SECRET) == "sk-secret-1234"

def test_decrypt_with_wrong_secret_raises():
    token = encrypt("abc", SECRET)
    import pytest
    with pytest.raises(Exception):
        decrypt(token, "1" * 64)

def test_mask_key():
    assert mask_key("sk-abcdefgh1234") == "sk-***1234"
    assert mask_key("short") == "***"
    assert mask_key("") == ""
```

- [ ] **步骤 2：运行验证失败**

运行：`.venv/bin/python -m pytest tests/test_crypto.py -q`
预期：FAIL（`ModuleNotFoundError: sophagent.crypto`）

- [ ] **步骤 3：实现**

```python
# sophagent/crypto.py
"""Symmetric encryption for provider API keys, keyed off config.secret.

The Fernet key is derived from the server secret with HKDF so the secret can
be any length; ciphertext is urlsafe-base64 text safe to store in a TEXT column.
"""
from __future__ import annotations

import base64

from cryptography.fernet import Fernet
from cryptography.hazmat.primitives import hashes
from cryptography.hazmat.primitives.kdf.hkdf import HKDF


def _fernet(secret: str) -> Fernet:
    raw = HKDF(algorithm=hashes.SHA256(), length=32, salt=None,
               info=b"sophagent-provider-key").derive(secret.encode("utf-8"))
    return Fernet(base64.urlsafe_b64encode(raw))


def encrypt(plaintext: str, secret: str) -> str:
    return _fernet(secret).encrypt(plaintext.encode("utf-8")).decode("ascii")


def decrypt(token: str, secret: str) -> str:
    return _fernet(secret).decrypt(token.encode("ascii")).decode("utf-8")


def mask_key(plaintext: str) -> str:
    if not plaintext:
        return ""
    if len(plaintext) <= 4:
        return "***"
    return f"{plaintext[:3]}***{plaintext[-4:]}"
```

- [ ] **步骤 4：加依赖**。在 `pyproject.toml` 的 `dependencies` 增加 `"cryptography>=42"`，运行 `uv pip install -p .venv/bin/python -e ".[dev]"`。

- [ ] **步骤 5：运行验证通过**

运行：`.venv/bin/python -m pytest tests/test_crypto.py -q`
预期：PASS（3 passed）

- [ ] **步骤 6：Commit**

```bash
git add sophagent/crypto.py tests/test_crypto.py pyproject.toml
git commit -m "feat(provider): 新增 apikey 对称加密模块 crypto"
```

## 任务 2：providers 表 + sessions 覆盖列 + DB 方法

**文件：**
- 修改：`sophagent/db.py`（SCHEMA、`_migrate_structure`、新增方法）
- 测试：`tests/test_registry.py`（本任务先验证 DB 层）

- [ ] **步骤 1：编写失败的测试**

```python
# tests/test_registry.py
import pytest
from sophagent.db import Database

@pytest.fixture
async def db(tmp_path):
    d = Database(tmp_path / "t.db")
    await d.connect()
    yield d
    await d.close()

async def test_provider_crud(db):
    await db.create_provider({"name": "p1", "api_mode": "openai",
        "base_url": "https://x/v1", "api_key_enc": "ENC", "context_limit": 64000})
    rows = await db.list_providers()
    assert [r["name"] for r in rows] == ["p1"]
    await db.update_provider("p1", {"api_mode": "openai", "base_url": "https://y/v1",
        "api_key_enc": "ENC2", "context_limit": 128000})
    row = await db.get_provider("p1")
    assert row["base_url"] == "https://y/v1" and row["context_limit"] == 128000
    await db.delete_provider("p1")
    assert await db.get_provider("p1") is None

async def test_session_override_columns(db):
    # minimal user/group/agent/session scaffolding
    uid = await db.create_user("u", "h", "admin")
    await db.ensure_groups()
    gid = (await db.get_owned_group(uid))["id"]
    aid = await db.create_agent({"name": "a", "system_prompt": "s",
        "provider": "p", "model": "m"}, uid, gid)
    await db.create_session("s1", uid, aid, gid)
    await db.set_session_overrides("s1", override_provider="p2",
        override_model="m2", thinking_mode="thinking")
    s = await db.get_session("s1")
    assert s["override_provider"] == "p2" and s["thinking_mode"] == "thinking"
```

`tests/test_registry.py` 顶部需要 `pytestmark = pytest.mark.asyncio` 或确认 `pytest-asyncio` 已配置（见 `pyproject.toml`；现有 `test_loop.py` 已用异步测试，沿用其模式）。

- [ ] **步骤 2：运行验证失败**

运行：`.venv/bin/python -m pytest tests/test_registry.py -q`
预期：FAIL（`AttributeError: 'Database' object has no attribute 'create_provider'`）

- [ ] **步骤 3：实现 SCHEMA + 迁移 + 方法**

在 `sophagent/db.py` 的 `SCHEMA` 字符串末尾追加：

```sql
CREATE TABLE IF NOT EXISTS providers (
  id INTEGER PRIMARY KEY,
  name TEXT UNIQUE NOT NULL,
  api_mode TEXT NOT NULL,
  base_url TEXT,
  api_key_enc TEXT,
  context_limit INTEGER NOT NULL DEFAULT 100000,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL
);
```

在 `_migrate_structure()` 末尾追加（守卫式 ALTER，仿现有 sessions 迁移）：

```python
        session_cols = await self._columns("sessions")
        for col in ("override_provider", "override_model", "thinking_mode"):
            if session_cols and col not in session_cols:
                await self._exec(f"ALTER TABLE sessions ADD COLUMN {col} TEXT")
```

在 db.py 的 sessions 区块之后新增 provider 区块：

```python
    # -- providers -----------------------------------------------------------

    async def create_provider(self, f: dict[str, Any]) -> int:
        cur = await self._exec(
            "INSERT INTO providers (name, api_mode, base_url, api_key_enc, context_limit,"
            " created_at, updated_at) VALUES (?,?,?,?,?,?,?)",
            (f["name"], f["api_mode"], f.get("base_url"), f.get("api_key_enc"),
             int(f.get("context_limit", 100_000)), now(), now()),
        )
        return cur.lastrowid

    async def get_provider(self, name: str) -> Optional[aiosqlite.Row]:
        return await self._one("SELECT * FROM providers WHERE name=?", (name,))

    async def list_providers(self) -> list[aiosqlite.Row]:
        return await self._all("SELECT * FROM providers ORDER BY name")

    async def update_provider(self, name: str, f: dict[str, Any]) -> None:
        # api_key_enc=None means "leave unchanged"
        if f.get("api_key_enc") is None:
            await self._exec(
                "UPDATE providers SET api_mode=?, base_url=?, context_limit=?, updated_at=?"
                " WHERE name=?",
                (f["api_mode"], f.get("base_url"), int(f["context_limit"]), now(), name),
            )
        else:
            await self._exec(
                "UPDATE providers SET api_mode=?, base_url=?, api_key_enc=?, context_limit=?,"
                " updated_at=? WHERE name=?",
                (f["api_mode"], f.get("base_url"), f["api_key_enc"],
                 int(f["context_limit"]), now(), name),
            )

    async def delete_provider(self, name: str) -> None:
        await self._exec("DELETE FROM providers WHERE name=?", (name,))
```

在 sessions 区块新增覆盖写入：

```python
    async def set_session_overrides(self, session_id: str, *, override_provider=None,
                                    override_model=None, thinking_mode=None) -> None:
        await self._exec(
            "UPDATE sessions SET override_provider=?, override_model=?, thinking_mode=?"
            " WHERE id=?",
            (override_provider, override_model, thinking_mode, session_id),
        )
```

> 注意：新增 `get_provider(name)` 与 users 区块无冲突（不同名 OK）。`create_agent` 测试里只传了部分字段，现有实现用 `.get` 容错，可正常工作。

- [ ] **步骤 4：运行验证通过**

运行：`.venv/bin/python -m pytest tests/test_registry.py -q`
预期：PASS（2 passed）

- [ ] **步骤 5：Commit**

```bash
git add sophagent/db.py tests/test_registry.py
git commit -m "feat(provider): providers 表与 sessions 覆盖列及 DB 方法"
```

## 任务 3：ProviderRegistry 单例

**文件：**
- 创建：`sophagent/providers/registry.py`
- 修改：`sophagent/config.py`（`ProviderConfig` 加 `source` 字段）
- 修改：`sophagent/providers/__init__.py`（`get_provider` 委托）
- 测试：`tests/test_registry.py`（追加）

- [ ] **步骤 1：config 加 source 字段**

`sophagent/config.py` 的 `ProviderConfig` 增加：

```python
@dataclass
class ProviderConfig:
    name: str
    api_mode: str
    api_key: str = ""
    base_url: str | None = None
    context_limit: int = 100_000
    source: str = "builtin"   # builtin (yaml) | db
```

- [ ] **步骤 2：编写失败的测试（追加到 tests/test_registry.py）**

```python
from sophagent.providers.registry import ProviderRegistry, ResolvedProvider
from sophagent.config import ProviderConfig
from sophagent.crypto import encrypt

SECRET = "0" * 64

async def test_registry_merges_db_over_builtin(db):
    builtin = {"p1": ProviderConfig(name="p1", api_mode="openai",
                                    base_url="https://builtin/v1", source="builtin")}
    await db.create_provider({"name": "p1", "api_mode": "openai",
        "base_url": "https://db/v1", "api_key_enc": encrypt("sk-db", SECRET),
        "context_limit": 50000})
    reg = ProviderRegistry(db, builtin_providers=builtin, secret=SECRET)
    await reg.refresh()
    r = reg.resolve("p1")
    assert r.source == "db" and r.base_url == "https://db/v1" and r.api_key == "sk-db"

async def test_registry_client_cache_rebuilds_on_change(db):
    await db.create_provider({"name": "p1", "api_mode": "openai", "base_url": None,
        "api_key_enc": encrypt("sk-1", SECRET), "context_limit": 100000})
    reg = ProviderRegistry(db, builtin_providers={}, secret=SECRET)
    await reg.refresh()
    c1 = reg.client("p1")
    assert reg.client("p1") is c1               # same fingerprint -> cached
    await db.update_provider("p1", {"api_mode": "openai", "base_url": "https://new/v1",
        "api_key_enc": encrypt("sk-2", SECRET), "context_limit": 100000})
    await reg.refresh()
    assert reg.client("p1") is not c1           # fingerprint changed -> rebuilt
```

- [ ] **步骤 3：运行验证失败** — `.venv/bin/python -m pytest tests/test_registry.py -q`，预期 FAIL（import error）。

- [ ] **步骤 4：实现 registry**

```python
# sophagent/providers/registry.py
"""Unified provider registry: built-in (yaml, read-only) + DB-managed, merged
with DB taking precedence. Module-level singleton, mirroring get_config()."""
from __future__ import annotations

import hashlib
import logging
import time
from dataclasses import dataclass
from typing import Any

from ..config import ProviderConfig
from ..crypto import decrypt
from .anthropic_provider import AnthropicProvider
from .base import Provider
from .openai_provider import OpenAIProvider

log = logging.getLogger(__name__)


@dataclass
class ResolvedProvider:
    name: str
    api_mode: str
    api_key: str
    base_url: str | None
    context_limit: int
    source: str

    def fingerprint(self) -> str:
        raw = f"{self.api_mode}|{self.base_url}|{self.api_key}|{self.context_limit}"
        return hashlib.sha256(raw.encode("utf-8")).hexdigest()[:16]


class ProviderRegistry:
    def __init__(self, db: Any, builtin_providers: dict[str, ProviderConfig], secret: str):
        self.db = db
        self.builtin = builtin_providers
        self.secret = secret
        self._resolved: dict[str, ResolvedProvider] = {}
        self._clients: dict[tuple[str, str], Provider] = {}
        self._models_cache: dict[str, tuple[float, list[str]]] = {}

    async def refresh(self) -> None:
        merged: dict[str, ResolvedProvider] = {}
        for name, c in self.builtin.items():
            merged[name] = ResolvedProvider(name, c.api_mode, c.api_key, c.base_url,
                                            c.context_limit, "builtin")
        for row in await self.db.list_providers():
            try:
                key = decrypt(row["api_key_enc"], self.secret) if row["api_key_enc"] else ""
            except Exception:
                log.warning("provider %r: api key decrypt failed (secret rotated?)", row["name"])
                key = ""
            merged[row["name"]] = ResolvedProvider(
                row["name"], row["api_mode"], key, row["base_url"],
                row["context_limit"], "db")
        self._resolved = merged

    def names(self) -> list[str]:
        return sorted(self._resolved)

    def resolve(self, name: str) -> ResolvedProvider:
        if name not in self._resolved:
            raise KeyError(f"unknown provider: {name!r}")
        return self._resolved[name]

    def client(self, name: str) -> Provider:
        # test/back-compat path: an explicitly injected client wins
        from . import _cache
        if name in _cache:
            return _cache[name]
        r = self.resolve(name)
        ckey = (name, r.fingerprint())
        if ckey not in self._clients:
            cfg = ProviderConfig(name=r.name, api_mode=r.api_mode, api_key=r.api_key,
                                 base_url=r.base_url, context_limit=r.context_limit)
            self._clients[ckey] = (AnthropicProvider(cfg) if r.api_mode == "anthropic"
                                   else OpenAIProvider(cfg))
        return self._clients[ckey]

    async def list_models(self, name: str, ttl: float = 300.0) -> list[str]:
        hit = self._models_cache.get(name)
        if hit and time.monotonic() - hit[0] < ttl:
            return hit[1]
        models = await _fetch_models(self.resolve(name))
        self._models_cache[name] = (time.monotonic(), models)
        return models


async def _fetch_models(r: ResolvedProvider) -> list[str]:
    if r.api_mode == "anthropic":
        from anthropic import AsyncAnthropic
        kw: dict = {"api_key": r.api_key or "missing"}
        if r.base_url:
            kw["base_url"] = r.base_url
        page = await AsyncAnthropic(**kw).models.list()
    else:
        from openai import AsyncOpenAI
        page = await AsyncOpenAI(api_key=r.api_key or "missing", base_url=r.base_url).models.list()
    return [m.id for m in getattr(page, "data", []) or []]


_registry: ProviderRegistry | None = None


def init_registry(db: Any, config) -> ProviderRegistry:
    global _registry
    _registry = ProviderRegistry(db, config.providers, config.secret)
    return _registry


def get_registry() -> ProviderRegistry:
    if _registry is None:
        raise RuntimeError("provider registry not initialized")
    return _registry


def reset_registry() -> None:
    global _registry
    _registry = None
```

- [ ] **步骤 5：`get_provider` 委托 registry**

改 `sophagent/providers/__init__.py`：

```python
def get_provider(name: str) -> Provider:
    if name in _cache:
        return _cache[name]
    from .registry import get_registry
    return get_registry().client(name)
```

保留 `_cache` 与 `reset_providers()` 不变（测试注入路径）。

- [ ] **步骤 6：运行验证通过**

运行：`.venv/bin/python -m pytest tests/test_registry.py -q`
预期：PASS（4 passed）

- [ ] **步骤 7：Commit**

```bash
git add sophagent/providers/registry.py sophagent/providers/__init__.py sophagent/config.py tests/test_registry.py
git commit -m "feat(provider): ProviderRegistry 合并解析与客户端缓存"
```

## 任务 4：启动接入 + runner/agent_routes 走 registry

**文件：**
- 修改：`sophagent/main.py`（lifespan 初始化 registry）
- 修改：`sophagent/agent/loop.py`（context_limit/client 改用 registry）
- 修改：`sophagent/api/agent_routes.py`（`_validate` 改查 registry）

- [ ] **步骤 1：main.py 初始化 registry**

在 `lifespan` 中 `app.state.db = db` 之后加：

```python
    from .providers.registry import init_registry
    registry = init_registry(db, cfg)
    await registry.refresh()
    app.state.providers = registry
```

并把第 59 行的 `if not cfg.providers:` 警告改为 `if not registry.names():`。

- [ ] **步骤 2：loop.py 用 registry 取 context_limit**

`AgentRunner.__init__` 中：

```python
        self.provider = get_provider(agent.provider)
        from ..providers.registry import get_registry
        self.context_limit = get_registry().resolve(agent.provider).context_limit
```

> 测试 client fixture 在 lifespan 启动时已 `init_registry`，但 `cfg.providers["test"]` 是在 create_app 之前注入的，会被 builtin 合并进 registry，故 `resolve("test")` 可用；`get_provider("test")` 命中 `_cache`。

- [ ] **步骤 3：agent_routes `_validate` 改查 registry**

```python
def _validate(req: AgentCreate) -> None:
    from ..providers.registry import get_registry
    names = get_registry().names()
    if req.provider not in names:
        raise HTTPException(400, f"unknown provider {req.provider!r}; configured: {names}")
    bad = set(req.tools) - set(all_tool_names())
    if bad:
        raise HTTPException(400, f"unknown tools: {sorted(bad)}; available: {all_tool_names()}")
```

同步把 `agent_options`（`/meta/options`）的 `"providers"` 改为 `get_registry().names()`。

- [ ] **步骤 4：运行全量测试确认零回归**

运行：`.venv/bin/python -m pytest -q`
预期：现有全部 PASS（registry 接入不破坏现有 provider 注入路径）。

- [ ] **步骤 5：Commit**

```bash
git add sophagent/main.py sophagent/agent/loop.py sophagent/api/agent_routes.py
git commit -m "feat(provider): 启动初始化 registry，runner 与校验改走 registry"
```

## 任务 5：Provider CRUD API + 模型列表端点

**文件：**
- 创建：`sophagent/api/provider_routes.py`
- 修改：`sophagent/api/__init__.py`、`sophagent/models.py`
- 测试：`tests/test_providers_api.py`

- [ ] **步骤 1：models.py 增 pydantic 模型**

```python
class ProviderCreate(BaseModel):
    name: str = Field(pattern=r"^[a-zA-Z0-9_.-]{1,64}$")
    api_mode: str = Field(pattern=r"^(openai|anthropic)$")
    base_url: Optional[str] = None
    api_key: str = ""
    context_limit: int = Field(default=100_000, ge=1)

class ProviderPatch(BaseModel):
    api_mode: str = Field(pattern=r"^(openai|anthropic)$")
    base_url: Optional[str] = None
    api_key: Optional[str] = None   # None/"" => 不变
    context_limit: int = Field(default=100_000, ge=1)
```

- [ ] **步骤 2：编写失败的测试**

```python
# tests/test_providers_api.py
def test_provider_crud_and_mask(client, admin):
    r = client.post("/api/providers", headers=admin, json={
        "name": "myp", "api_mode": "openai", "base_url": "https://x/v1",
        "api_key": "sk-abcdefgh1234", "context_limit": 64000})
    assert r.status_code == 201, r.text
    lst = client.get("/api/providers", headers=admin).json()
    me = next(p for p in lst if p["name"] == "myp")
    assert me["api_key"] == "sk-***1234" and me["source"] == "db"
    # builtin 'test' present and read-only
    bt = next(p for p in lst if p["name"] == "test")
    assert bt["source"] == "builtin"
    r = client.put("/api/providers/myp", headers=admin, json={
        "api_mode": "openai", "base_url": "https://y/v1", "context_limit": 128000})
    assert r.status_code == 200
    assert client.delete("/api/providers/myp", headers=admin).status_code == 200

def test_provider_write_requires_admin(client, bob):
    r = client.post("/api/providers", headers=bob, json={
        "name": "np", "api_mode": "openai", "api_key": "k"})
    assert r.status_code == 403

def test_cannot_modify_builtin(client, admin):
    assert client.put("/api/providers/test", headers=admin, json={
        "api_mode": "openai", "context_limit": 1}).status_code == 400
    assert client.delete("/api/providers/test", headers=admin).status_code == 400
```

- [ ] **步骤 3：运行验证失败** — `.venv/bin/python -m pytest tests/test_providers_api.py -q`，预期 404/FAIL（路由不存在）。

- [ ] **步骤 4：实现路由**

```python
# sophagent/api/provider_routes.py
from fastapi import APIRouter, Depends, HTTPException, Request

from ..auth import require_user
from ..config import get_config
from ..crypto import encrypt, mask_key
from ..models import ProviderCreate, ProviderPatch
from ..providers.registry import get_registry

router = APIRouter()


async def _require_admin(request: Request, user) -> None:
    if not await request.app.state.db.is_admin(user["id"]):
        raise HTTPException(403, "admin only")


@router.get("")
async def list_providers(request: Request, user=Depends(require_user)):
    await _require_admin(request, user)
    reg = get_registry()
    out = []
    for name in reg.names():
        r = reg.resolve(name)
        out.append({"name": r.name, "api_mode": r.api_mode, "base_url": r.base_url,
                    "api_key": mask_key(r.api_key), "context_limit": r.context_limit,
                    "source": r.source})
    return out


@router.post("", status_code=201)
async def create_provider(req: ProviderCreate, request: Request, user=Depends(require_user)):
    await _require_admin(request, user)
    db = request.app.state.db
    if await db.get_provider(req.name) is not None:
        raise HTTPException(409, "provider name already exists")
    enc = encrypt(req.api_key, get_config().secret) if req.api_key else None
    await db.create_provider({"name": req.name, "api_mode": req.api_mode,
        "base_url": req.base_url, "api_key_enc": enc, "context_limit": req.context_limit})
    await get_registry().refresh()
    return {"ok": True, "name": req.name}


@router.put("/{name}")
async def update_provider(name: str, req: ProviderPatch, request: Request, user=Depends(require_user)):
    await _require_admin(request, user)
    db = request.app.state.db
    if await db.get_provider(name) is None:
        raise HTTPException(400, "cannot modify a non-DB (built-in) or missing provider")
    enc = encrypt(req.api_key, get_config().secret) if req.api_key else None
    await db.update_provider(name, {"api_mode": req.api_mode, "base_url": req.base_url,
        "api_key_enc": enc, "context_limit": req.context_limit})
    await get_registry().refresh()
    return {"ok": True}


@router.delete("/{name}")
async def delete_provider(name: str, request: Request, user=Depends(require_user)):
    await _require_admin(request, user)
    db = request.app.state.db
    if await db.get_provider(name) is None:
        raise HTTPException(400, "cannot delete a non-DB (built-in) or missing provider")
    await db.delete_provider(name)
    await get_registry().refresh()
    return {"ok": True}


@router.get("/{name}/models")
async def list_models(name: str, request: Request, user=Depends(require_user)):
    await _require_admin(request, user)
    try:
        return {"models": await get_registry().list_models(name)}
    except Exception as e:
        return {"models": [], "error": str(e)}
```

挂载（`sophagent/api/__init__.py`）：import `provider_routes` 并在 `mount_routes` 加
`app.include_router(provider_routes.router, prefix="/api/providers", tags=["providers"])`。

- [ ] **步骤 5：运行验证通过** — `.venv/bin/python -m pytest tests/test_providers_api.py -q`，预期 PASS（3 passed）。

- [ ] **步骤 6：Commit**

```bash
git add sophagent/api/provider_routes.py sophagent/api/__init__.py sophagent/models.py tests/test_providers_api.py
git commit -m "feat(provider): provider CRUD 与模型列表 API（admin）"
```

## 任务 6：前端 Provider 管理卡片 + model 下拉

**文件：**
- 修改：`web/index.html`

- [ ] **步骤 1：renderAdmin 增 Providers 卡片**

在 `renderAdmin()` 的 skills 卡片之前插入（用现有 `card/table/row/btn` 助手）：

```js
  // providers card (REQ3.1)
  const pc = card(box, "Providers (模型供应商 — admin)");
  const providers = await apiJson("/api/providers");
  const pt = table(pc, ["Name", "Mode", "Base URL", "Key", "Ctx", "Source", ""]);
  for (const p of providers) {
    const tr = row(pt, [p.name, p.api_mode, p.base_url || "—", p.api_key || "—",
                        p.context_limit, p.source]);
    const td = tr.insertCell(); td.innerHTML = `<div class="row-actions"></div>`;
    if (p.source === "db") {
      btn(td.firstChild, "edit", "ghost", () => providerForm(pc, p));
      btn(td.firstChild, "delete", "danger", async () => {
        if (!confirm(`Delete provider ${p.name}?`)) return;
        await apiJson("/api/providers/" + p.name, {method: "DELETE"}).catch(alertErr);
        renderAdmin();
      });
    }
  }
  const pnb = document.createElement("div"); pnb.style.marginTop = "10px"; pc.appendChild(pnb);
  btn(pnb, "+ New provider", "", () => providerForm(pc, null));
```

新增 `providerForm`（仿 `agentForm`）：

```js
function providerForm(parent, existing) {
  const old = parent.querySelector(".provider-form"); if (old) old.remove();
  const f = document.createElement("div"); f.className = "provider-form";
  f.style.cssText = "margin-top:12px;border-top:1px solid var(--border);padding-top:8px";
  f.innerHTML = `
    <div class="grid2">
      <div><label>Name</label><input id="pf-name"></div>
      <div><label>API mode</label><select id="pf-mode">
        <option value="openai">openai</option><option value="anthropic">anthropic</option></select></div>
      <div><label>Base URL</label><input id="pf-url" placeholder="https://api.example.com/v1"></div>
      <div><label>Context limit</label><input id="pf-ctx" type="number" value="100000"></div>
      <div><label>API key${existing ? " (留空=不变)" : ""}</label><input id="pf-key" type="password"></div>
    </div>
    <div style="margin-top:12px;display:flex;gap:8px">
      <button id="pf-save"></button><button class="ghost" id="pf-cancel">Cancel</button></div>`;
  parent.appendChild(f);
  if (existing) {
    $("pf-name").value = existing.name; $("pf-name").disabled = true;
    $("pf-mode").value = existing.api_mode; $("pf-url").value = existing.base_url || "";
    $("pf-ctx").value = existing.context_limit;
  }
  $("pf-save").textContent = existing ? "Save" : "Create";
  $("pf-cancel").onclick = () => f.remove();
  $("pf-save").onclick = async () => {
    const body = {api_mode: $("pf-mode").value, base_url: $("pf-url").value.trim() || null,
                  context_limit: +$("pf-ctx").value, api_key: $("pf-key").value};
    try {
      if (existing) await apiJson("/api/providers/" + existing.name, {method: "PUT", body});
      else { body.name = $("pf-name").value.trim();
             await apiJson("/api/providers", {method: "POST", body}); }
      renderAdmin();
    } catch (e) { alert(e.message); }
  };
}
```

- [ ] **步骤 2：model 输入改为 datalist 联动（REQ3.2）**

在 `agentForm` 的 model 输入旁加 datalist，并在 provider 选择 change 时拉模型：

```js
// 把 model input 改为带 datalist：
//   <input id="af-model" list="af-models" placeholder="...">
//   <datalist id="af-models"></datalist>
async function fillModels(provider, datalistId) {
  const dl = $(datalistId); dl.innerHTML = "";
  try {
    const {models} = await apiJson("/api/providers/" + provider + "/models");
    for (const m of models || []) { const o = document.createElement("option"); o.value = m; dl.appendChild(o); }
  } catch (e) { /* 静默回退到手填 */ }
}
// 在 agentForm 内绑定： $("af-provider").onchange = () => fillModels($("af-provider").value, "af-models");
// 并在初始化后调用一次 fillModels(当前 provider, "af-models")
```

> 注意：`/api/providers/{name}/models` 仅 admin 可调。非 admin 用户在 Agents 页编辑时该请求会 403 → `fillModels` 的 catch 静默回退到手填，符合预期；模型下拉是锦上添花。

- [ ] **步骤 3：手动验证**（无前端测试框架）

```
ADMIN_PASSWORD=dev .venv/bin/uvicorn sophagent.main:app --reload
```
1. admin 登录 → Admin 页 → Providers 卡片出现，含 builtin 行（只读）。
2. New provider 填 openai + key → 列表显示掩码 key、source=db。
3. edit 留空 key 保存 → key 不变；改 base_url 生效。
4. Agents 页选该 provider → model 输入出现候选下拉（端点可用时）。

- [ ] **步骤 4：Commit**

```bash
git add web/index.html
git commit -m "feat(web): Provider 管理卡片与 model 下拉"
```

---

# 里程碑 M2 已并入 M1 任务 5/6（模型列表端点 + 前端下拉）

> 模型列表（REQ3.2）实现量小，已随 CRUD 一并落地，无独立里程碑。

---

# 里程碑 M3：会话级覆盖 + 思考/快速模式

## 任务 7：协议层 + 适配器思考映射

**文件：**
- 修改：`sophagent/providers/base.py`、`openai_provider.py`、`anthropic_provider.py`
- 修改：`tests/conftest.py`（`EchoProvider.chat` 加 `**kwargs`）

- [ ] **步骤 1：conftest EchoProvider 接受 thinking/记录 kwargs**

```python
    async def chat(self, *, model, system, messages, tools=None,
                   temperature=None, max_tokens=None, thinking=None) -> AsyncIterator:
        self.last_kwargs = {"model": model, "thinking": thinking, "temperature": temperature}
        ...
```

- [ ] **步骤 2：base.py 加参数**

`Provider.chat` 签名加 `thinking: str | None = None`，docstring 注明取值 `"thinking"|"fast"|None`。

- [ ] **步骤 3：OpenAI 适配器映射 + 容错**

`OpenAIProvider.chat` 加 `thinking=None`；在组装 kwargs 后：

```python
        if thinking == "thinking":
            kwargs["reasoning_effort"] = "high"
        try:
            stream = await self.client.chat.completions.create(
                **kwargs, stream_options={"include_usage": True})
        except TypeError:
            kwargs.pop("reasoning_effort", None)
            stream = await self.client.chat.completions.create(**kwargs)
```

（保留原有 `stream_options` 容错；`reasoning_effort` 不被支持时一并去除重试。）

- [ ] **步骤 4：Anthropic 适配器映射**

`AnthropicProvider.chat` 加 `thinking=None`；组装 kwargs 时：

```python
        if thinking == "thinking":
            kwargs["thinking"] = {"type": "enabled", "budget_tokens": 4096}
            kwargs.pop("temperature", None)  # extended thinking 要求不自定义 temperature
```

- [ ] **步骤 5：运行全量测试确认零回归** — `.venv/bin/python -m pytest -q`，预期全 PASS。

- [ ] **步骤 6：Commit**

```bash
git add sophagent/providers/base.py sophagent/providers/openai_provider.py sophagent/providers/anthropic_provider.py tests/conftest.py
git commit -m "feat(provider): chat() 思考模式参数与各 provider 原生映射"
```

## 任务 8：会话覆盖贯通 runner + PATCH 路由

**文件：**
- 修改：`sophagent/agent/runtime.py`、`sophagent/agent/loop.py`、`sophagent/api/session_routes.py`、`sophagent/models.py`
- 测试：`tests/test_overrides_usage.py`

- [ ] **步骤 1：models.py 加 SessionOverridePatch**

```python
class SessionOverridePatch(BaseModel):
    override_provider: Optional[str] = None
    override_model: Optional[str] = None
    thinking_mode: Optional[str] = Field(default=None, pattern=r"^(default|thinking|fast)$")
```

- [ ] **步骤 2：编写失败的测试**

```python
# tests/test_overrides_usage.py
def test_session_override_thinking_passed_to_provider(client, bob, agent_id):
    s = client.post("/api/sessions", headers=bob, json={"agent_id": agent_id}).json()
    client.patch(f"/api/sessions/{s['id']}", headers=bob,
                 json={"thinking_mode": "thinking", "override_model": "big-model"})
    r = client.post(f"/api/sessions/{s['id']}/chat", headers=bob, json={"content": "hi"})
    assert r.status_code == 200
    assert client.provider.last_kwargs["thinking"] == "thinking"
    assert client.provider.last_kwargs["model"] == "big-model"
```

- [ ] **步骤 3：运行验证失败** — 预期 FAIL（PATCH 路由不存在 / 未传 thinking）。

- [ ] **步骤 4：实现 PATCH + runner 贯通**

`session_routes.py` 新增：

```python
from ..models import AgentDef, ChatRequest, SessionCreate, SessionOverridePatch

@router.patch("/{session_id}")
async def patch_session(session_id: str, req: SessionOverridePatch, request: Request,
                        user=Depends(require_user)):
    db = request.app.state.db
    if await db.get_session(session_id, user["id"]) is None:
        raise HTTPException(404, "session not found")
    await db.set_session_overrides(session_id, override_provider=req.override_provider,
        override_model=req.override_model, thinking_mode=req.thinking_mode)
    return dict(await db.get_session(session_id, user["id"]))
```

`chat()` worker 中构造 runner 时传入覆盖（session 行已加载，含覆盖列）：

```python
                runner = await build_runner(
                    db=db, skill_store=state.skill_store, agent=agent,
                    user_id=user["id"], history=history, on_persist=persist,
                    override_provider=session["override_provider"],
                    override_model=session["override_model"],
                    thinking_mode=session["thinking_mode"],
                )
```

`runtime.build_runner` 加参并应用 effective 值：

```python
async def build_runner(*, db, skill_store, agent, user_id, history, on_persist=None,
                       depth=0, override_provider=None, override_model=None, thinking_mode=None):
    ...
    eff = AgentDef(**{**agent.__dict__,
                      "provider": override_provider or agent.provider,
                      "model": override_model or agent.model})
    runner = AgentRunner(eff, ctx, history, on_persist=on_persist)
    runner.thinking = (thinking_mode if thinking_mode and thinking_mode != "default" else None)
    return runner
```

`AgentRunner.__init__` 末尾加 `self.thinking: str | None = None`；`_call_model` 调用处传 `thinking=self.thinking`：

```python
                async for ev in self.provider.chat(
                    model=self.agent.model, system=system, messages=self.history,
                    tools=tool_schemas or None, temperature=self.agent.temperature,
                    thinking=self.thinking):
```

> `AgentDef` 是 dataclass，`agent.__dict__` 可展开重建；effective agent 仅覆盖 provider/model，其余字段（tools/skills 等）原样。

- [ ] **步骤 5：运行验证通过** — `.venv/bin/python -m pytest tests/test_overrides_usage.py -q`，预期 PASS。

- [ ] **步骤 6：Commit**

```bash
git add sophagent/agent/runtime.py sophagent/agent/loop.py sophagent/api/session_routes.py sophagent/models.py tests/test_overrides_usage.py
git commit -m "feat(session): 会话级 provider/model/思考模式覆盖贯通 runner"
```

## 任务 9：前端会话工具栏

**文件：**
- 修改：`web/index.html`

- [ ] **步骤 1：composer 上方加工具栏 DOM**

在 `#composer` 内 `#chips` 之前插入：

```html
<div id="sesstools" style="display:flex;gap:8px;align-items:center;font-size:12px;color:var(--dim)">
  <select id="st-provider" style="width:auto"></select>
  <input id="st-model" list="st-models" placeholder="model" style="width:160px">
  <datalist id="st-models"></datalist>
  <select id="st-think" style="width:auto">
    <option value="default">默认</option><option value="thinking">思考</option>
    <option value="fast">快速</option></select>
</div>
```

- [ ] **步骤 2：openSession 回显 + 绑定持久化**

在 `openSession` 中（拿到 `detail` 后）填充工具栏：provider 选项来自 `currentAgent` 可见的 provider（普通用户可调 `/api/agents/meta/options` 拿 names），值优先 `detail.override_provider || currentAgent.provider`；model 同理；`st-think` 取 `detail.thinking_mode || "default"`。三个控件 `onchange` 调：

```js
async function saveOverrides() {
  await apiJson("/api/sessions/" + currentSid, {method: "PATCH", body: {
    override_provider: $("st-provider").value || null,
    override_model: $("st-model").value.trim() || null,
    thinking_mode: $("st-think").value}}).catch(()=>{});
}
```

provider change 时同时 `fillModels($("st-provider").value, "st-models")`（admin 才有数据，普通用户静默回退手填）。

- [ ] **步骤 3：手动验证** — 切换 provider/model/思考模式 → 刷新页面后保持；发消息时 agent 用新值（可用思考模型观察 reasoning 出现）。

- [ ] **步骤 4：Commit**

```bash
git add web/index.html
git commit -m "feat(web): 会话工具栏切换 provider/model/思考模式"
```

---

# 里程碑 M4：用量 / 上下文 / 缓存命中率

## 任务 10：usage 纯函数 + 适配器读缓存 token

**文件：**
- 创建：`sophagent/usage.py`
- 修改：`sophagent/models.py`（`AssistantTurn` 加字段）、`openai_provider.py`、`anthropic_provider.py`
- 测试：`tests/test_overrides_usage.py`（追加）

- [ ] **步骤 1：编写失败的测试（追加）**

```python
from sophagent.usage import cache_hit_percent

def test_cache_hit_percent():
    assert cache_hit_percent(0, 100) is None
    assert cache_hit_percent(50, 0) is None
    assert cache_hit_percent(30, 100) == 30
    assert cache_hit_percent(200, 100) == 100   # 截断
```

- [ ] **步骤 2：运行验证失败** — import error。

- [ ] **步骤 3：实现 usage.py（照搬 hermes 口径）**

```python
# sophagent/usage.py
"""Prompt-cache hit percent: cached reads over the full prompt total
(input + cache reads + cache writes). Kept server-side so all displays agree."""
from __future__ import annotations


def cache_hit_percent(cache_read: int, prompt_total: int):
    cache_read = int(cache_read or 0)
    prompt_total = int(prompt_total or 0)
    if cache_read <= 0 or prompt_total <= 0:
        return None
    return min(100, round(cache_read / prompt_total * 100))
```

- [ ] **步骤 4：AssistantTurn 加字段**

```python
@dataclass
class AssistantTurn:
    content: str = ""
    tool_calls: list[ToolCall] = field(default_factory=list)
    input_tokens: int = 0
    output_tokens: int = 0
    cache_read_tokens: int = 0
    cache_write_tokens: int = 0
    stop_reason: str = ""
    reasoning: str = ""
```

- [ ] **步骤 5：OpenAI 适配器读 cached_tokens**

在 `async for chunk` 的 usage 分支：

```python
            if getattr(chunk, "usage", None):
                input_tokens = chunk.usage.prompt_tokens or 0
                output_tokens = chunk.usage.completion_tokens or 0
                details = getattr(chunk.usage, "prompt_tokens_details", None)
                if details is not None:
                    cache_read_tokens = getattr(details, "cached_tokens", 0) or 0
```

`cache_read_tokens = 0` 在循环前初始化；`turn_done` 的 `AssistantTurn(...)` 传 `cache_read_tokens=cache_read_tokens`（OpenAI 无单独 cache write，留 0）。

- [ ] **步骤 6：Anthropic 适配器读 cache token + 启用缓存**

`message_start`：`input_tokens = usage.input_tokens or 0`，并读
`cache_read_tokens = getattr(usage, "cache_read_input_tokens", 0) or 0`、
`cache_write_tokens = getattr(usage, "cache_creation_input_tokens", 0) or 0`。
给 system 与 tools 末块加 `cache_control`：

```python
        if system:
            kwargs["system"] = [{"type": "text", "text": system,
                                 "cache_control": {"type": "ephemeral"}}]
        if tools:
            t = tools_to_anthropic(tools)
            t[-1]["cache_control"] = {"type": "ephemeral"}
            kwargs["tools"] = t
```

`turn_done` 传 `cache_read_tokens`/`cache_write_tokens`。

- [ ] **步骤 7：运行验证通过** — `.venv/bin/python -m pytest tests/test_overrides_usage.py -q`，预期 PASS。

- [ ] **步骤 8：Commit**

```bash
git add sophagent/usage.py sophagent/models.py sophagent/providers/openai_provider.py sophagent/providers/anthropic_provider.py tests/test_overrides_usage.py
git commit -m "feat(usage): 适配器读取缓存 token 与缓存命中率算法"
```

## 任务 11：loop 发 turn_usage + done 扩展

**文件：**
- 修改：`sophagent/agent/loop.py`
- 测试：`tests/test_overrides_usage.py`（追加）

- [ ] **步骤 1：编写失败的测试（追加，用脚本化 turn）**

```python
def test_turn_usage_and_done_usage(client, bob, agent_id):
    from sophagent.models import AssistantTurn
    client.provider.script = [AssistantTurn(content="hi", stop_reason="stop",
        input_tokens=100, output_tokens=20, cache_read_tokens=40)]
    s = client.post("/api/sessions", headers=bob, json={"agent_id": agent_id}).json()
    r = client.post(f"/api/sessions/{s['id']}/chat", headers=bob, json={"content": "yo"})
    events = [l for l in r.text.splitlines() if l.startswith("data: ")]
    import json
    payloads = [json.loads(l[6:]) for l in events]
    turn_usage = next(p for p in payloads if p["type"] == "turn_usage")
    assert turn_usage["cache_read"] == 40 and turn_usage["cache_hit"] == 29  # 40/140
    done = next(p for p in payloads if p["type"] == "done")
    assert done["usage"]["input_tokens"] == 100
    assert "context_length" in done and "context_limit" in done
```

- [ ] **步骤 2：运行验证失败** — 无 `turn_usage` 事件。

- [ ] **步骤 3：实现**

`AgentRunner.__init__` 的 `self.usage` 扩展：

```python
        self.usage = {"input_tokens": 0, "output_tokens": 0,
                      "cache_read_tokens": 0, "cache_write_tokens": 0}
```

`run()` 在累加后、判断 tool_calls 之前 yield per-turn 事件：

```python
            self.usage["input_tokens"] += turn.input_tokens
            self.usage["output_tokens"] += turn.output_tokens
            self.usage["cache_read_tokens"] += turn.cache_read_tokens
            self.usage["cache_write_tokens"] += turn.cache_write_tokens
            from ..usage import cache_hit_percent
            turn_total = turn.input_tokens + turn.cache_read_tokens + turn.cache_write_tokens
            yield {"type": "turn_usage", "input": turn.input_tokens,
                   "output": turn.output_tokens, "cache_read": turn.cache_read_tokens,
                   "cache_hit": cache_hit_percent(turn.cache_read_tokens, turn_total)}
```

两处 `done` 事件改为带上下文与累计缓存：

```python
            if not turn.tool_calls:
                yield {"type": "done", "usage": dict(self.usage),
                       "context_length": history_tokens(system, self.history),
                       "context_limit": self.context_limit,
                       "cache_hit": cache_hit_percent(self.usage["cache_read_tokens"],
                           self.usage["input_tokens"] + self.usage["cache_read_tokens"]
                           + self.usage["cache_write_tokens"])}
                return
```

末尾 `max_iterations` 的 done 同样补这些字段（DRY：抽一个 `_done_payload(system)` 辅助方法返回 dict，两处复用）。

```python
    def _done_payload(self, system: str) -> dict:
        from ..usage import cache_hit_percent
        u = self.usage
        return {"type": "done", "usage": dict(u),
                "context_length": history_tokens(system, self.history),
                "context_limit": self.context_limit,
                "cache_hit": cache_hit_percent(u["cache_read_tokens"],
                    u["input_tokens"] + u["cache_read_tokens"] + u["cache_write_tokens"])}
```

- [ ] **步骤 4：运行验证通过** — `.venv/bin/python -m pytest tests/test_overrides_usage.py -q`，预期 PASS。

- [ ] **步骤 5：Commit**

```bash
git add sophagent/agent/loop.py tests/test_overrides_usage.py
git commit -m "feat(usage): loop 发 turn_usage 并在 done 扩展上下文与缓存字段"
```

## 任务 12：前端状态栏 + 每轮标签

**文件：**
- 修改：`web/index.html`

- [ ] **步骤 1：DOM + CSS**

在 `#sesstools` 旁或 composer 底部加状态栏：

```html
<div id="usagebar" style="font-size:11px;color:var(--dim);display:flex;gap:14px;padding:2px 0">
  <span id="ub-tok">↑0 ↓0</span>
  <span id="ub-ctx">ctx 0/0</span>
  <span id="ub-cache">cache —</span>
</div>
```

CSS 加 `.turnmeta{font-size:11px;color:var(--dim);align-self:flex-start;margin-top:-6px;opacity:.8}`。

- [ ] **步骤 2：sendMsg 事件循环消费新事件**

```js
        } else if (ev.type === "turn_usage") {
          const meta = document.createElement("div");
          meta.className = "turnmeta";
          meta.textContent = `本轮 ↑${ev.input} ↓${ev.output}` +
            (ev.cache_hit != null ? ` · 缓存 ${ev.cache_hit}%` : "");
          msgs.appendChild(meta);
          bubble = null; thinkEl = null;     // 下一轮重新计
        } else if (ev.type === "done") {
          const u = ev.usage || {};
          $("ub-tok").textContent = `↑${u.input_tokens||0} ↓${u.output_tokens||0}`;
          $("ub-ctx").textContent = `ctx ${ev.context_length||0}/${ev.context_limit||0}`;
          $("ub-cache").textContent = ev.cache_hit != null ? `cache ${ev.cache_hit}%` : "cache —";
        }
```

> 注意：现有循环里 `done` 未被显式处理（靠流结束），新增分支不影响 `finally`。`turn_usage` 在每个 assistant turn 后到达；放在 `text_delta`/`tool_call` 处理之后不冲突。

- [ ] **步骤 3：openSession 重置状态栏**

打开会话时把 `ub-*` 置为 `—`/0（历史不回算）。

- [ ] **步骤 4：手动验证**
1. 发消息 → 状态栏 token/ctx 更新；助手回复下方出现「本轮 …」标签。
2. 多轮工具调用 → 每轮一个标签。
3. 支持缓存的 provider 连续对话 → cache 百分比 > 0。

- [ ] **步骤 5：Commit**

```bash
git add web/index.html
git commit -m "feat(web): 用量状态栏与每轮用量标签"
```

---

## 收尾：全量验证

- [ ] 运行 `.venv/bin/python -m pytest -q`，预期全部 PASS（含新增 4 个测试文件）。
- [ ] 按 docker 测试配置（providers=sophnet/DeepSeek-V4-Flash，端口 8848）做一次真实链路冒烟：建 db provider → 拉模型 → 思考模式对话 → 观察用量/缓存。
- [ ] 使用 superpowers:finishing-a-development-branch 收尾分支。

---

## 自检结果

**规格覆盖度：**
- REQ3.1 provider 入库切换 → 任务 1–6（crypto/表/registry/CRUD/UI）；会话级覆盖 → 任务 8–9。✅
- REQ3.2 模型列表 → 任务 5（端点）+ 6/9（前端下拉）。✅
- REQ3.3 思考/快速 → 任务 7（映射）+ 8（贯通）+ 9（工具栏）。✅
- REQ3.4 用量/上下文/缓存 → 任务 10（缓存读取/算法）+ 11（事件）+ 12（UI）。✅
- 安全：加密入库 + 掩码（任务 1/5）；admin 鉴权（任务 5）。✅
- 边界：解密失败容错（任务 3）、builtin 只读（任务 5）、思考参数容错（任务 7）、缓存缺失→null（任务 10/11）。✅

**占位符扫描：** 无 TODO/待定；每个代码步骤含真实代码。「快速模式后期细化」为已确认决策。✅

**类型一致性：** `ResolvedProvider`/`ProviderRegistry`/`AssistantTurn` 新字段在跨任务引用处命名一致；`set_session_overrides`、`build_runner` 新参数、`cache_hit_percent` 签名前后统一；`EchoProvider.last_kwargs` 在任务 7 定义、任务 8 使用。✅
